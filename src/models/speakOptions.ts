export interface SpeakOptions {
    /**
     * Speech rate, a value between -10 (very slow) and 10 (very fast). 
     */
    rate?: number

    /**
     *  Volume of the Speech, a value between 0 and 100.
     */
    volume?: number
}